# El Campo LARD Controller (Home Assistant add-on)

Supervised Braiins board-priority actuator for the S19j Pro (LARD).

- `enable_writes` defaults to **false** (observe/heartbeat only)
- Host network; health on port 8099
- Does not expose miner/inverter ports publicly

Private ops repo for Home Assistant OS local install via Supervisor store.
